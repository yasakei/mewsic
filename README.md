# Legacy TypeScript version (unsupported)

This branch contains the legacy TypeScript implementation of the app.

**No support is provided for this version.** It is kept for reference only.

The current version of the project is **Mewsic** — see the `main` branch.
